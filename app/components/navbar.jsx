// Placeholder file retained temporarily while cleaning up the app/components folder.
// This file intentionally does not export a default React component to avoid Next
// creating a page/route for `/components/navbar` during prerender. The real
// Navbar component lives at `components/navbar.jsx` and app pages import that.

export const __removedPlaceholder = true;

// TODO: delete this file once all references are confirmed to use `@/components`.