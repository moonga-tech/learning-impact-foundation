// Placeholder file retained temporarily while cleaning up the app/components folder.
// To prevent Next from generating a page/route for `/components`, this file
// intentionally does not export a default React component.

export const __removedPlaceholder = true;

// TODO: remove when it's safe to delete the whole folder.
