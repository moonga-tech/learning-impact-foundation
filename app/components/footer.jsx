// Placeholder file retained temporarily while cleaning up the app/components folder.
// The real Footer lives in `components/footer.jsx`. This file intentionally
// avoids exporting a default React component to prevent Next from creating
// a `/components/footer` route during prerender.

export const __removedPlaceholder = true;

// TODO: delete when safe.